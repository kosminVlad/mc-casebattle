# Инструкция по миграции и запуску

## ✅ Что было сделано

1. ✅ Создана структура проекта React с Vite
2. ✅ Настроен `package.json` с зависимостями
3. ✅ Настроен роутинг с `react-router-dom`
4. ✅ Все страницы перенесены в `src/pages/`
5. ✅ Все компоненты обновлены (убраны `'use client'`, заменены импорты)
6. ✅ Настроен Tailwind CSS
7. ✅ Создан `App.tsx` с роутингом
8. ✅ Настроены конфигурационные файлы (vite.config.ts, tsconfig.json)

## 📦 Установка зависимостей

```bash
npm install
```

Или используйте ваш менеджер пакетов:
- `yarn install`
- `bun install`

## 🚀 Запуск проекта

```bash
# Dev-сервер (http://localhost:5173)
npm run dev

# Production сборка
npm run build

# Предпросмотр production сборки
npm run preview
```

## 🔄 Изменения в коде

### Роутинг
- `next/navigation` → `react-router-dom`
- `useRouter()` → `useNavigate()`
- `useParams()` → `useParams()` из react-router
- `Link` из next/link → `Link` из react-router-dom

### Структура
- `app/page.tsx` → `src/pages/HomePage.tsx`
- `app/case/[slug]/page.tsx` → `src/pages/CasePage.tsx`
- Все страницы в `src/pages/`

### Компоненты
- Убраны все директивы `'use client'`
- Обновлены импорты роутинга

## 📝 Важные файлы

- `src/main.tsx` - точка входа
- `src/App.tsx` - главный компонент с роутингом
- `vite.config.ts` - конфигурация Vite
- `index.html` - HTML шаблон
- `app/globals.css` - глобальные стили (импортируется в main.tsx)

## ⚠️ Примечания

- Все компоненты остались без изменений (кроме импортов)
- Логика работы полностью сохранена
- Стили и анимации работают как прежде
- Все хуки и сервисы работают без изменений

